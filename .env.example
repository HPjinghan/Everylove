# 复制本文件为 .env.local 并填入真实 key（.env.local 已被 .gitignore 忽略，不会进 git）。
# 改动 .env.local 后需重启 npx expo start 才生效（Metro 打包时内联）。
# 注意：EXPO_PUBLIC_ 变量会打进客户端包——仅限试装自测；分发包不带 key、由登录后的服务端代理供给（见 DECISIONS D-004/D-010/D-057）。
# 引擎与 key **只从这里读**（D-069）：设置页的手填与脚本引擎（mock）已下线；本地没 key 时走服务端代理（未登录自动用匿名游客身份，D-088）；代理也连不上才显示「未配置 AI」。
# 代码里只有 core/config.ts 读这些变量（D-086）：要加一项配置，先在那里登记（必须写成字面量的 process.env.EXPO_PUBLIC_XXX，Metro 才会内联），再来这里补说明。

# Claude（https://console.anthropic.com/settings/keys）
EXPO_PUBLIC_ANTHROPIC_API_KEY=

# Claude 模型（可选，默认 claude-sonnet-5；可换 claude-opus-5 / claude-haiku-4-5，D-108）
# EXPO_PUBLIC_ANTHROPIC_MODEL=claude-sonnet-5

# 百度千帆（https://console.bce.baidu.com/qianfan/ → API Key）
EXPO_PUBLIC_QIANFAN_API_KEY=

# 千帆上用哪个模型，默认 deepseek-v4-pro；以千帆模型广场上的模型 ID 为准
# EXPO_PUBLIC_QIANFAN_MODEL=deepseek-v4-pro

# 用哪家引擎（D-069）：anthropic | qianfan。不填则有 Claude key 用 Claude，否则千帆（服务端代理两家都通）。
# EXPO_PUBLIC_AI_ENGINE=qianfan

# 图像生成（立绘 / 外出拍照）与聊天共用上面的千帆 key，无需单独配置。
# 文生图模型，默认 qwen-image；以千帆模型广场为准
# EXPO_PUBLIC_QIANFAN_IMAGE_MODEL=qwen-image

# 语音合成（TA 的语音气泡，D-048/D-073）：百度短文本语音合成 tsn.baidu.com/text2audio，同一把千帆 key。
# 音色 per 默认按角色人称（他 4193 度泽言 / 她 4194 度嫣然 / TA 4115 度小贤），可全局覆盖：
# EXPO_PUBLIC_BAIDU_TTS_PER=4194

# 她的语音识别（D-073）：百度 ASR vop.baidu.com，同一把千帆 key。默认普通话极速版 80001、界面英语时 1737；可强制：
# EXPO_PUBLIC_BAIDU_ASR_DEV_PID=1537

# 她发的照片（D-073）：千帆视觉模型客观描述后交给聊天模型；默认 qwen3.5-397b-a17b，可换（如 qwen3.5-35b-a3b 更快更便宜）：
# EXPO_PUBLIC_QIANFAN_VISION_MODEL=qwen3.5-397b-a17b

# ── 她的语音识别：Whisper 协议通道（D-139，可选）——日 / 韩必须；中 / 英默认仍走上面的百度 ──
# 任何 Whisper 协议的服务都行：Groq（https://console.groq.com，whisper-large-v3-turbo，最便宜最快）/ OpenAI（whisper-1、gpt-4o-mini-transcribe）/ 硅基流动 …
# EXPO_PUBLIC_ASR_BASE_URL=https://api.groq.com/openai/v1
# EXPO_PUBLIC_ASR_API_KEY=
# EXPO_PUBLIC_ASR_MODEL=whisper-large-v3-turbo
# 哪些界面语言走这条通道（其余走百度）；all = 全部
# EXPO_PUBLIC_ASR_LANGS=ja,ko

# ── TA 的声音：Fish Audio（D-139，可选）——中 / 英 / 日 / 韩全语种、每个角色自己的音色（创造 ⑧）；不配则百度（只会中 / 英） ──
# https://fish.audio → API keys。模型：s2.1-pro（默认）/ s2.1-pro-free（免费期到 2026-11-30）/ s1
# EXPO_PUBLIC_FISH_API_KEY=
# EXPO_PUBLIC_FISH_MODEL=s2.1-pro
# 兜底音色 id（角色没选、音色池里也没这门语言时按人称取）；不填 = Fish 默认声
# EXPO_PUBLIC_FISH_VOICE_HE=
# EXPO_PUBLIC_FISH_VOICE_SHE=
# EXPO_PUBLIC_FISH_VOICE_TA=
# 音色池 content/voices.ts 由 npm run fish-voices 从 Fish 声库拉取（用上面的 key），人工筛过再提交。
# 分发包走代理时，同名配置放 Supabase Secrets：ASR_BASE_URL / ASR_API_KEY / ASR_MODEL、FISH_API_KEY / FISH_MODEL（音色仍由客户端决定）

# 账号与云备份（D-054）：Supabase 项目设置 → API 里的 Project URL 与 anon public key。
# 建表 SQL 见 docs/supabase-setup.sql；不配置时账号功能隐藏、其他一切照旧。
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
