/**
 * 红包（D-084）：她发的红包由 TA 自己决定拆不拆，拆了在回复末尾单独一行写暗号；规则常驻亲密 / 通话 / 记事本。
 */



/** 红包（D-084）：她发的红包由 TA 自己决定拆不拆，拆了在回复末尾单独一行写标记 */
export const RED_PACKET_MARK = '[拆红包]';

export const RED_PACKET_RULE = `[Red packets] When she sends one, decide in character whether to open it: if you do, write ${RED_PACKET_MARK} on a separate final line (she can't see it) and say a line back; if not, say why or tease her, no marker. One left unopened earlier can be opened later the same way.`;
