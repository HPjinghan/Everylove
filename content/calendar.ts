/**
 * 日历（D-020 / D-110）内容：世界层节假日（中文盘 → 中国节日；日语盘上线时按市场换 JP 表）。
 * 任何年份都有内容（D-110）：公历固定日子按规则算，农历节日与清明按年查表（2026–2035，之后补表）。
 * 世界层只做标记，不做调休。
 */

/** 公历固定日子：MM-DD → 节日名（任何年份） */
const FIXED: Record<string, string> = {
  '01-01': '元旦',
  '02-14': '情人节',
  '03-08': '妇女节',
  '05-01': '劳动节',
  '05-20': '520',
  '06-01': '儿童节',
  '10-01': '国庆',
  '11-11': '双十一',
  '12-24': '平安夜',
  '12-25': '圣诞节',
};

/** 按年查表的日子：除夕 / 春节 / 清明 / 端午 / 七夕 / 中秋（公历 MM-DD） */
const BY_YEAR: Record<number, Record<string, string>> = {
  2026: { '02-16': '除夕', '02-17': '春节', '04-05': '清明', '06-19': '端午', '08-19': '七夕', '09-25': '中秋' },
  2027: { '02-05': '除夕', '02-06': '春节', '04-05': '清明', '06-09': '端午', '08-08': '七夕', '09-15': '中秋' },
  2028: { '01-25': '除夕', '01-26': '春节', '04-04': '清明', '05-28': '端午', '08-26': '七夕', '10-03': '中秋' },
  2029: { '02-12': '除夕', '02-13': '春节', '04-04': '清明', '06-16': '端午', '08-16': '七夕', '09-22': '中秋' },
  2030: { '02-02': '除夕', '02-03': '春节', '04-05': '清明', '06-05': '端午', '08-05': '七夕', '09-12': '中秋' },
  2031: { '01-22': '除夕', '01-23': '春节', '04-05': '清明', '06-24': '端午', '08-24': '七夕', '10-01': '中秋' },
  2032: { '02-10': '除夕', '02-11': '春节', '04-04': '清明', '06-12': '端午', '08-12': '七夕', '09-19': '中秋' },
  2033: { '01-30': '除夕', '01-31': '春节', '04-04': '清明', '06-01': '端午', '08-01': '七夕', '09-08': '中秋' },
  2034: { '02-18': '除夕', '02-19': '春节', '04-05': '清明', '06-20': '端午', '08-20': '七夕', '09-27': '中秋' },
  2035: { '02-07': '除夕', '02-08': '春节', '04-05': '清明', '06-10': '端午', '08-10': '七夕', '09-16': '中秋' },
};

/** 兼容旧引用：2026 全表 */
export const CN_HOLIDAYS_2026: Record<string, string> = Object.fromEntries([
  ...Object.entries(FIXED).map(([md, name]) => [`2026-${md}`, name]),
  ...Object.entries(BY_YEAR[2026]).map(([md, name]) => [`2026-${md}`, name]),
]);

export function holidayFor(dateKey: string): string | undefined {
  const [y, md] = [Number(dateKey.slice(0, 4)), dateKey.slice(5)];
  const lunar = BY_YEAR[y]?.[md];
  const fixed = FIXED[md];
  if (lunar && fixed) return `${fixed}・${lunar}`;
  return lunar ?? fixed;
}

/** Date → 'YYYY-MM-DD'（本地时区） */
export function dateKey(d: Date): string {
  const y = d.getFullYear();
  const m = (d.getMonth() + 1).toString().padStart(2, '0');
  const day = d.getDate().toString().padStart(2, '0');
  return `${y}-${m}-${day}`;
}

/** 'YYYY-MM-DD' → 本地 Date（当天 00:00） */
export function parseDateKey(key: string): Date {
  const [y, m, d] = key.split('-').map(Number);
  return new Date(y, m - 1, d);
}
