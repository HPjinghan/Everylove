# everylove（中文代号：全自动恋爱）— CLAUDE.md

> 自己捏人的乙女游戏：进来捏一个人，他全自动和你恋爱，不用你主动。
> 族谱：c.ai 的皮 · 乙游的心脏 · 短剧的钱包 · 独有器官 = 主动性。
> 一句话：c.ai 证明了胃口，乙游证明了付费，没人把两者接起来过。

* 状态（2026-09-10）：试装 v0.2——手机壳桌面 + 13 个模块可跑，全部界面已按纸面设计系统重做，TestFlight 构建号 4（SDK 57）在测；最近一条决策 D-110。**本文件只写现行口径**：每条决策的理由、编号索引、被推翻的历史都在 `docs/DECISIONS.md`（按主题合并的总账，头部有 D-编号索引），合并前的逐条原文冻结在 `docs/archive/`。
* 本文档是产品的单一事实来源。**执行任何任务时产生的新设计决策，必须当次写进文档**（见「工作规则」）。
* 本项目与团队其他产品无关，不引入其他项目的术语与范式。

---

## 1\. 定位与基调

* 全自动恋爱 / 零劳动被爱：他主动，用户视角 POV，不入镜。
* 尺度：暧昧合规线
* 世界基调：一个无条件接纳你的世界——广场上人人都接你的话。
* 性向：**架构全性向 + 非人类，火力女频先行**。门对所有人开，灯先打一束。
  * onboarding 一步问清：昵称必填 + **「更倾向于和什么样的人建立关系？」**（男生 / 女生 / 都可以 / 非人类）必选——既是全性向声明，也是交友口味过滤；性别 / 称呼 / 职业 / 生日可跳过，其余在「设置 → 我的身份」补充，可为单个角色使用不同身份。
  * 男性向内容是后置的第二战役（另一门手艺、另一种付费结构），不在 v1 火力内。
* 市场：目标日本；中文版先行、只验证机制。

## 2\. 商业承重墙（双层结构）

* **广场层（免费）**（机制词；产品形态 = 交友 App 的滑卡配对 + 外出「广场」的陌生人偶遇）：撩是免费的。免费层角色**故意不完整**——有一点兴趣但不主动，配对 3 天不聊过期（TA 忘记你）。免费层的天花板是商业决策，不是产品缺陷。
* **建立羁绊层（付费）**：被爱是收费的。主动性、记忆、他的社媒、故事、显影全部只在这层。槽位：首个免费，加槽付费（试装模拟：Free 1 / Pro 5 / Max 不限）。
* 收费结构：**订阅管「他在」**（完整日常 + 语音 + 留言），**按件管名场面**（电话、漫画集、贺卡、纪念日特辑、平行世界点单、morning call、错过回溯——后两者原与「他有作息」咬合，作息已下线、形态待拍板 OPEN_QUESTIONS #20）。每笔消费必须存进「你的他 + 你的故事史」这个资产。
* 窗口纪律：周级迭代、第一天收钱、任何 SKU 不做一次性消耗品。

## 3\. 信息架构（手机壳桌面）

**主页 = 一部手机的桌面**，无 bottom bar。纪律：**模块必须有内容供给才上架，无供给不摆图标；做少而真、不做多而假**（点不动的按钮比没有更出戏）。

**桌面**：纯色底 + 菱格暗纹——壁纸只换纸的颜色不换纸，**壁纸即主题**（「纸面」= 设计系统原色，默认；其余 5 款各一块浅底 + 配套浅色，同一套暗纹，不渐变不分段；选一款桌面、Dock 图块与里面每一屏一起变），4 列图标网格**水平翻页**（格位跨页连续，页码点在 Dock 上方，编辑模式拖到边缘自动翻页）+ 大时钟 + **大天气卡**（Open-Meteo 真实天气，可点开定位 / 搜索、7 日预报；位置只存本机；无位置回落种子假天气）；长按进入编辑模式自由摆放；底部 iPhone 式 **Dock**（默认 通讯录 + 设置，最多 4 个，可拖入拖出）；顶部未读横幅：一人未读直达会话、多人合并「A、B · N 条新消息」进 Message；首次加好友后**揭幕**三卡（一次性）。图标 MingCute 白底 ink 单色。闹钟（morning call）、音乐（v1.5，站外播放卡片）未上架；系统级拟真彩蛋（锁屏照片 / 壁纸 / 铃声）留正式版。

**上架 13 个模块**：

1. **Message**：白色通栏列表 + primary 未读角标；会话 = 纸面聊天流（accentSoft 底 + 涂鸦壁纸、TA 白气泡 / 我 primary 气泡、Fredoka 时间戳与已读、ink 系统条）；LV1 首次进会话插一条「+」面板预告（白底 accent 字，一次性）。消息能力：文本 / 图片 / 语音 / 引用 / 撤回 24h 占位 / 删除仅本地。**她的语音与照片 TA 真的听到 / 看到**（语音转文字、照片视觉描述作为上下文交给聊天引擎，均按一次开口计心动 / XP；识别中 / 失败在气泡下露出）。**TA 偶尔发语音**（只在羁绊会话，只挑最后一条短气泡，概率随主动联系强度、她刚发过语音更爱回）。**「+」面板**（羁绊会话）：外出邀请（地点 → 时间，卡片即约定、TA 答应）/ 查 TA 的手机（锁屏，第一次要密码：自己猜或「问 TA 要密码」，TA 按性格 × 亲密度决定给不给）/ 红包（游戏币，拆不拆由 TA 决定）/ 位置（真实地图：定位、点选或搜索）。**纯打字感**：TA 只发会真的打出来的字，无（）动作——情景是外出独有的语法。**约定识别**：聊天里定下「什么时候在哪见面」自动进日程与外出约定，到点没去 = 爽约，TA 记得并主动说一句。会话顶栏「羁绊 LVn · 阶段 ›」进 **TA 的主页**（统计卡 + 电话 / 查手机 / 约 TA 三个入口 + 信息卡：称呼 / 纪念日 / 生日 / 秘密进度；他的社媒 / 故事页未做）。
2. **电话**：可通话的人 = 加好友的 TA。真通话 = 管线式（TA 接起先开口 → 音量计断句听她说 → 识别 → 通话模式引擎 → 合成 → 再听），免提 / 听筒即时切换，每轮约 4~8 秒；通话内容进羁绊会话并计 XP，挂断留「📞 m:ss」并提取记忆。TA 主动来电（来电流）未做；端到端实时语音要等 dev build（#26）。
3. **X**：只有缔结的 TA 们的时间线，推特式白通栏行布局。可赞可评，TA 的回帖走模型（可多次，暗面路由前置）。**评论区不只有她**：每条帖子由 TA 身边的人（§3-11 圈子）与其他缔结的 TA 来评论 1–3 条、TA 可回一句（模型写，AI 不可用回落通用反应）；点头像开 TA 的资料页；在广场见过的 TA 的公开帖标「在广场见过」。**发帖调度器**：频率按 MBTI（E 多 I 少，无 MBTI 1 条/天），内容由模型按时段 + 天气 + 羁绊记忆 + 最近的帖与记事本生成——**TA 自己的时间线**（工作、朋友、爱好、小麻烦，人和事前后一致），她的影子出现多少按分量、不点名她；启动 / 回前台补投、错过只补一条。「偷看」的家。
4. **交友**：Tinder 式滑卡（白卡描边 3:4）——左滑略过（冷却 3 天回流；略过后 3 秒内可原位「撤销」；全池被略过时忽略冷却，划完自动重开）、右滑心动 **TA 必同意**、右滑即配对 → 试聊（心动条吸顶、顶栏「还剩 N 天」倒计时、offer 卡在输入栏上方）；顶部配对头像条；口味 chip 随时改偏好（直接过滤牌池）；滑卡 / 列表（双列瀑布流，点卡即配对）分段可切。牌堆走推荐打分（热度 log 压缩 / 新面孔 / 每日轮换 / 略过冷却；UGC 进同一池同一套）。**心动值**：试聊中她每开口上涨（步长 = 100 / 角色节奏，±15%），满 100 = TA 开口交换联系方式 → 缔结。预告卡不进牌堆；自己的创作不进自己的牌堆；搜索与 chips 不做。
5. **通讯录**：缔结的人 + 自创角色的「心动中」暧昧期条目（不占槽、不过期，副行显示心动进度；满 100 缔结才占槽）。无分组标题。
6. **相册**：拍立得墙——外出拍的照片按下快门、洗好即入册（白框相纸 + 手写字 + 小倾角，点开可分享）。
7. **日历**：真实日历三层——世界层（分市场节假日，**任何年份都有**：公历固定日子按规则、农历节日按年查表 2026–2035）/ 关系层（**每一位 TA** 的「和 X 交换联系方式」「和 X 的一百天」「X 的生日」、她的生日 + 带时间的约定，约定条目右侧「赴约 ›」直达外出；网格下方三层**图例**）/ 用户层（手动日程，每条触发心跳三段式：前一天 18:00 事前关心、当天 7:00 加油、次日 12:00 回访，错过不补投）。**只记安排与纪念日**，TA 经历的事在发生之后进记事本或 X。不读系统日历；用户日程按最高敏感级；生理期关怀 v1 不做（#15）。
8. **外出**：顶部「广场」banner = 偶遇**陌生人**（还没加好友的角色；TA 不认识她，只注入「我的边界」；偶遇也积累心动、与试聊互通，满 100 当场交换联系方式）。六个地点（咖啡馆 / 公园 / 书店 / 电影院 / 游乐园 / 海边）：**有约定** → 赴约（约定时间前 2h～后 3h 内 TA 在等，准时 / 早到 / 迟到（容差 10 分钟）TA 都知道；过窗口没去 = 爽约，留记录、写记忆、TA 主动说一句）；**没约定** → 偶遇通讯录里的一位。场景 = **亲身互动的故事模式**：TA 台词 +（）现场描写、她的（）视为动作、**TA 先开口（开场白走模型，每次不一样；失败回落模板不重复）**、不分条；点头像 / 顶栏副文开 **TA 的资料页**（立绘、身份、钩子、自介、世界、见过面）；陌生人偶遇结束留一条**偶遇记录**（TA 记得在广场见过她：进初识 / 广场 prompt、资料页与 X）；发消息 +XP（陌生人除外）。**会话续上**：没点结束再进同一地点还是 TA，一小时没说话才刷新；换地点先结束上一场；结束时留「你们一起去了××」并把对话并进记忆。外出页的约定只在天气条下方的**约定条**表达一次（最多 2 条、其余折叠；长按取消）。**拍照**：📸 合影（她只入镜侧影 / 手，不画清晰正脸）/ 📷 拍 TA，她按快门生图 → 拍立得，洗好即进相册。顶部带当天天气。
9. **创造**：**描述导入**（≤2000 字，自动解析成表单，AI 不可用回落规则解析）+ 表单——名字 / 性别 / 长相 / 背景 / **年龄状态**（必选，未成年不能发布 #18）/ **形象必选**（上传图 或 生成立绘，不收真人照片）/ **画风八选一**（动漫走蒸汽机约 10 秒、其余 qwen-image 约 1 分钟；立绘与外出拍照共用）/ **谁能遇到 TA**（私密 / 公开 → 共享池）/ **TA 所在的世界**（现实世界 + 世界书里收藏的）；高级：种族 / 生日 / 口癖 / 喜欢 / 讨厌 / 确定关系节奏 / 恋爱类型 14 种 / MBTI / 聊天设定 / 作息 / 预设共同记忆 / 主动联系强度 / 禁忌 / 隐藏设定（LV3 起逐级解锁，未解锁不进 prompt）。发布时模型写一次 **TA 的台词**（开场白 / 想确定关系时 / 确定关系后前三条 / 一句人设 / 追法），写不成回落原型兜底。**我创建的**：独立列表页（创造页顶栏右侧入口、设置也能进），编辑回填全部字段与台词、可让 TA 重写；自编辑即时生效于自己的实例，他人领养快照不变。
10. **记事本**：她自己的本子，列表 + 全屏编辑；**私密**——只有「让 TA 看我的手机」时 TA 才读到（§7 可选日记通道），并进记忆。
11. **查手机**：每个缔结的 TA 一部手机。「看 TA 的手机」= iPhone 式锁屏（同 Message「+」；「问 TA 要密码」后锁屏不关，TA 的回复原地以气泡显示）→ **一部真的手机**：角色色桌面上摆 TA 的 App（Message / 通讯录 / 记事本 / 日历 / 相册），点进去看。**TA 身边的人**（圈子）：第一次打开时模型按人设生成一次（4–6 个家人 / 朋友 / 同事 + 和其中 3 人的近期聊天，写不成回落通用圈子），通讯录与 Message 里不只有她；圈子进记事本 / 发帖 / 亲密 prompt（提到时前后一致），X 里由他们来评论。TA 的**记事本**（TA 自己的日子：工作、朋友、家人、爱好、小麻烦，身边的人前后一致；她出现多少按「她在 TA 心里的分量」——恋爱脑常常是她、冷静的人偶尔一笔；按 MBTI 频率写，I 比 E 勤；锁着的页 = 隐藏设定）+ **日历**（约定 + TA 生日）+ **Message**（和她的对话）+ **相册**。「让 TA 看我的手机」：先弹二次确认（会读到什么、看完会发消息、不可撤回），TA 翻她的记事本与她和其他 TA 的近期聊天，发 1–2 句（只说自己感受、不审问不愧疚绑架、其他真人一字不评、记事本先过暗面路由）。「被发现」留 #19。
12. **设置**：账号 · 云端（登录 / 立即备份 / 从云端恢复 / 退出 / 删除云端数据）、语言、我的身份、主题（**只剩壁纸一排** = 纸面 + 5 款换色、暗纹常在，一步到位全局生效；配色四套已下线）、订阅计划（试装模拟；槽位超额时数值标 accent 并说明不能再新增）、素材开关、我的创作、开发者（**AI 引擎点选切换**（本机偏好）、取路只读、记忆库查看、立绘重画、重置）。
13. **世界书**：TA 所处的世界与 TA 对一切的认知。**现实世界（当前）内置为默认**（不进 prompt）；可创建别的世界（名字 / 一句话 / 设定一行一条）、浏览、收藏，**只有收藏的世界能选给角色**（创造表单「TA 所在的世界」）；非现实世界作为【你所在的世界】注入所有 prompt（对话 / 外出 / 通话 / 记事本 / 发帖 / 身边的人）；删除世界 = 住在里面的 TA 回到现实世界。

**关键流**：

* **领养流**：心动满 → 缔结仪式（交友 = 交换联系方式；自创 = 确定关系）→ 槽位判定（对所有羁绊生效）→ 只问一句「TA 叫——」（称呼 = 她的昵称，生日在身份里）→ 迁移仪式动画 → 落桌面（首次揭幕，TA 的第一句话在未读横幅里）→ 第一次入册后强制登录。
* **分享流**（系统分享面板「转给他」）、**来电流**（定时 / 事件触发全屏响铃）：未做。

## 4\. 首十分钟

onboarding（语言 → 先让 TA 们认识你；第一步底部「已有账号？登录」直接接回云端存档、不重走）→ **直接落交友滑卡，不见桌面**（顶部「先逛逛」逃生门）→ 右滑心动、TA 必同意 → 试聊上头 → 心动满交换联系方式 → 缔结仪式 → **桌面揭幕**「这部手机，现在是你们的了」+ 三张模块卡（Message / 创造 / 外出，一次性可跳过），TA 的第一句话躺在未读横幅里等她点进去 → 第一次入册后强制登录。开门与离席已下线，首十分钟新的情绪终点与 aha 待重定义（#20）。首次付费提示不出现在首十分钟，留给第一个情绪峰值。

## 5\. 角色系统（行为树四层）

* **系统层（锁死，任何自定义不可覆盖）**：情绪暗面路由（四语触发、按语言回复）、真人检测、尺度钳、行为健康底线（不纠缠、不刷屏、不用愧疚感惩罚离开——能捏疯批台词，不能捏 PUA 行为）。
* **原型层（官方蒸馏）**：蒸馏的不是口癖，是**追法**——亲密度阶梯曲线即性格。首发四棵：温柔年上、毒舌竹马、霸总、**官方人外一棵**（龙 / 神明 / 非人执事谱系内选定，待拍板 #2）。每棵 = 触发器集 + 追法曲线 + 台词风格包。试装种子已扩为 6 位（2男2女1龙1狐，中 / 英 / 日 / 韩各一套，内置立绘），原型树蒸馏与人外正式人设仍待拍板。
* **创作层（UGC 两档）**：大众捏皮（创造表单，§3-9）；高阶捏树（填空卡片改触发器 / 事件 / 追法，未做）。发布审核：**挡 IP 角色、挡真人**（现为本地名单拦截 + 自我声明，平台审核待接 #7）。
* **个体层**：每段关系独立状态机——**羁绊等级** LV1 刚认识 → LV6 唯一例外（升级门槛 50 / 90 / 130 / 170 / 210），**XP 唯一来源 = 她每开口一次 +5**（文字 / 语音 / 照片 / 卡片 / 电话每句 / 外出同口径，陌生人无 XP），升级出系统提示；记忆库；秘密解锁进度。**领养快照制**：创作者更新不改写已领养实例。

## 6\. 内容四维度

* **日常（他在）**：TA 随时在线随时回；准时消息、语音、对你分享内容的留言。TA 有自己的生活：记事本与 X 里是 TA 自己的日子，她占多少按**「她在 TA 心里的分量」**（`lib/her-share.ts`：恋爱类型 → 追法家族兜底，叠 MBTI 与主动联系强度，0.1～0.9 三档）——恋爱脑满脑子是她，冷静的人她只是生活的一部分。作息与「他主动来找你」的节奏待拍板（#12 / #20）。
* **故事（他是谁）**：他的主线按章连载——**半成品剧本库**（编剧写原型骨干章节，生成缝进用户的名字、日常、选择）；「我们的故事」名场面按件；**平行世界 AU 点单**。未做。
* **相册（值得）**：卡面级美术，少而精，集中预算。会话内生图已整体下线；生成图只余立绘与外出拍照（拍立得）——图是场景本身的呈现，不包装成「他画的 / 他送的」。
* **关系网（有戏）**：周边角色 + 群聊修罗场——多智能体自演、用户潜水看戏，**事件制**（控成本，大型修罗场可为付费名场面）。所有角色围着用户转，不做角色动物园。未做。

## 7\. 素材系统

* 全部可选开关，**开得越多他越懂你**——隐私授权即亲密度进度条。
* 主通道：**分享**（用户发完帖顺手转给他；绕开反爬、API 费用与日本鍵垢问题，未做）。辅助：选项口味、聊天内容、可选日记（= **记事本**，她让 TA 看手机时 TA 才读到，并进记忆）。
* **他只看她**：分享内容里出现的任何其他真人，永不评论。

## 8\. 创作者经济

* 上传原创角色 → 他人领养 → **创作者分成**（未做）；「热度」排行榜（界面口径为小火苗 + 热度，「领养」仅作机制词，不出现在用户可见文案）。
* **共享角色池**已落地：公开的自创角色进所有玩家的交友牌堆（同语言、同一套推荐打分），卡面标「来自其他玩家」；审核待接。
* 供给侧冷启动：定向招募现有社区的头部女性向捏人创作者。

## 9\. 红线（不可违反，不可被任何任务修改）

1. 男主/角色永不真人化：不收真人照片、不爬任何真人社媒、不做明星。用户文字描述「神似」可以。
2. 他只看她：不评论用户分享内容中的其他真人。
3. 情绪暗面路由前置：用户素材中出现痛苦/危机内容时走独立温柔模式，绝不入戏；素材数据（日记、分享）按最高敏感级处理。
4. IP 角色不做，UGC 审核强制拦截（含日本市场事務所风险）。
5. 尺度借既有线（晋江 / 女性向音声惯例），不自己发明尺度。
6. 行为无 PUA：不纠缠、不刷屏、不愧疚绑架。除非角色设定如此
7. 免费层不得偷偷变好（见商业承重墙）。

## 10\. 指标

漏斗：配对开聊率 → 领养转化率 →（北极星待拍板，原「八点开门率」随开门下线失去载体，#20）→ 订阅 / 首件转化 → 复购。窗口期健康：首单转化 + 复购，不看裸 DAU。

## 11\. 工作规则（对 Claude Code 的要求）

1. **决策入档纪律（最重要）**：每次执行任务，凡产生**新的设计决策**——无论产品、机制、数值、文案规范还是技术选型——必须在同一次任务内写进文档，不留「下次补」：
   * `docs/DECISIONS.md` 是**按主题合并的总账**：编号继续递增，在头部索引表加一行；改写它所属主题的条目——推翻旧口径 = 原地替换并在「曾经」留一行（编号 + 一句话），没有合适主题就新开一条；**不逐条追加**。旧编号的完整原文在 `docs/archive/`，不再改动。
   * 同步更新本文件受影响章节；**本文件只写现行口径**，不保留被推翻的原文（历史去总账查）。
2. **产品级开放问题不得自行拍板**：涉及定位、定价、尺度、红线、首发范围的问题写入 `docs/OPEN_QUESTIONS.md` 等 Harper 决定；纯技术实现决策可自行决定，但同样按第 1 条入档。
3. **红线章节（第 9 节）只有 Harper 明示才能修改。**
4. **界面 token 纪律**：新界面与改动只引 `constants/design.ts`（Shape / Type / Space / Pattern）与 `constants/theme.ts` 的 `Romance` / `Fonts` token（半透明遮罩用 `withAlpha`），不手写 hex、圆角、阴影、字号、描边，无阴影无渐变；内容卡片一律 `components/card.tsx` 的 `Card`，按钮 `Button`、选项 `Chip` / `Segmented`、表单 `Field` / `Input`、背景暗纹 `paper-bg`；**Fredoka（`Fonts.label` / `labelBold`）只用于数字与拉丁标签，所有中文走系统字体**。
5. **底座纪律**：`core/` 不认识任何具体玩法（出现「if 红包」「if 千帆」就是放错地方）；**新玩法 = `features/` 里一个文件**，往插槽注册并在 `features/index.ts` 登记；界面只调 `lib/chat.ts` 与 `features/*` 的 send 函数，不直接 import 引擎、记忆、约定识别；新行为挂扩展点、不改 `core/turn.ts` 的管线；工程配置只在 `core/config.ts` 读。**改到模型看到的字，`npm test` 的快照必红**——确认 diff 是想要的再更新。prompt 文本只进 `content/prompts/` 对应用途的文件，一段只属于一个用途。菜谱见 `docs/ARCHITECTURE.md` §5。
6. **文案纪律**：界面提示只描述内容或情绪、不解释机制、不指路、不预告交互方式（保留红线 / 政策告知、隐私与数据说明、商业信息）；用户可见文案不出现「领养」；新文案先过这条，再写 `t('中文')` 并在 `lib/i18n.ts` 尾部哨兵前补 en / ja / ko 词典——`npm test` 的词典覆盖测试会扫源码，缺词即红。

## 12\. 待拍板清单

见 `docs/OPEN_QUESTIONS.md`（#1–#26；已了结的条目在原位标注）。

## 13\. 技术栈与工程（现行；细节与编号见 `docs/DECISIONS.md` A–H）

* **客户端**：Expo **SDK 57**（2026-09-09 自 54 升级，D-102；RN 0.86 / React 19.2 / TS 6，只有新架构；升 SDK 走 `expo install expo@<ver>` → `expo install --fix` → `expo-doctor`）+ React Native + TypeScript，expo-router；**只做 iOS**；试装 Expo Go（`npx expo start` 扫码）。界面强制浅色。
* **分发**：Expo Go 朋友试装 = EAS Update `preview` 渠道（发布时置空 AI key）；TestFlight = EAS Build `production` 档 → `eas submit`（`eas.json`，bundle id `com.kotoko.everylove`，构建号远程自增，`--auto-submit`）。动了 app.json 插件 / 原生依赖必须重新 build + submit；JS 改动 `eas update --channel production` 热更。步骤见 `docs/RELEASE.md`。
* **目录**：`core/`（底座：registry / hooks / config / providers / prompt / modes / markers / cards / jobs / turn）+ `features/`（一个玩法一个文件，`features/index.ts` 启动清单）+ `lib/`（chat 会话入口、engine 门面、memory、media、tts、imagegen、outing、appointments、call、posts、his-notes、circle 身边的人、worlds 世界书、phone、pool、auth、sync、proxy、weather、i18n、bond、recommend）+ `content/`（`characters/` 四语种子与脚本、`prompts/` 一用途一文件、`places` / `calendar` / `portraits` / `worlds`）+ `store/app-store.ts`（zustand + AsyncStorage，persist v6）+ `app/`（桌面 `index.tsx`、`apps/*` 模块、`chat` / `bond` / `outing` / `call` / `adopt` / `auth` / `onboarding` / `weather`）+ `components/`。手册 `docs/ARCHITECTURE.md`。
* **AI 取路**：本地 key 直连（`.env.local`：`EXPO_PUBLIC_ANTHROPIC_API_KEY` / `EXPO_PUBLIC_QIANFAN_API_KEY`，可选 `EXPO_PUBLIC_AI_ENGINE`；设置 → 开发者可点选供应商覆盖，只存本机）> 有会话走服务端代理（`supabase/functions/ai`，真账号或匿名游客，每人每日 500 次）> 不可用抛错——**失败不回落，直接露出原因**（顶部轻提示停 1 秒，不写进会话，D-110）。供应商 anthropic（模型 `EXPO_PUBLIC_ANTHROPIC_MODEL`，默认 `claude-sonnet-5`；Opus 5 默认开思考，回话自动加余量 + effort low）/ qianfan（千帆 v2 OpenAI 兼容，模型默认 `deepseek-v4-pro`）在 `features/providers.ts` 注册。暗面路由、尺度、无 PUA 在引擎入口执行，任何供应商不可绕过。
* **对话**：四模式（初识 / 亲密 / 外出 / 通话）各自独立 prompt，系统 prompt 按分段表装配（`core/prompt.ts`，分段在 `features/prompts.ts` 声明；世界书 / 身边的人 / 偶遇记录三段 D-110）；**模型调用失败 = 顶部轻提示停 1 秒，不写进会话**（语音 / 照片识别失败、X 回帖失败同口径）；聊天两模式纯打字感（禁（），`stripStageDirections` 兜底）、1-2 句短句口语、亲密可拆两条气泡；外出独享（）描写、不分条；输出语言按界面语言。上下文 = 最近 20 轮（`HISTORY_ROUNDS`）；语音 / 照片 / 卡片经 `messageContextText` 进上下文。
* **记忆**：`lib/memory.ts` mem0 式本地实现，`Bond.memory = { facts ≤30, summary }`，每 3 个用户轮次后台提取、旧对话滚进摘要，注入亲密 / 外出 / 通话；外出对话、电话、爽约、她的记事本也并入。**只有羁绊层有记忆**。正式版切自托管 mem0，接口不变。
* **语音**：她的语音 → OpenAI 兼容通道优先（Whisper 协议，`EXPO_PUBLIC_SPEECH_*`，中 / 英 / 日）→ 回落百度 ASR（同一把千帆 key，不支持日语，#25）；TA 的语音 → OpenAI 兼容 `/audio/speech` 或百度 `text2audio`（音色按人称 4193 / 4194 / 4115）；`shouldSendVoice` 决定 TA 偶尔发语音。通话页 `app/call/[characterId].tsx` 管线式；端到端实时语音等 dev build（#26）。
* **图像**：只剩**立绘**与**外出拍照**，千帆文生图；prompt = 画风行 → 主体（不写角色名）→ `PORTRAIT_SYSTEM` + 末行红线句 + 反向提示；六位种子角色用各自的专属画风行与主体段（`content/prompts/portrait-seeds.ts`，D-107）；画风表 `PORTRAIT_STYLES`（`Character.artStyle`，缺省少女漫·水彩），动漫 → 蒸汽机 Air-Image（专用端点 / 代理 `qianfan.musesteamer`）、其余 → qwen-image。种子角色内置立绘 `assets/portraits/`（`scripts/gen-seed-portraits.mts`），取用 `portraitSource` / `portraitFor`（本机 > 内置）。调 prompt 工具：双击 `gen-image.bat`（127.0.0.1:3939）或 `npm run gen-image`，实时读工程画风表。
* **天气 / 地图**：`lib/weather.ts` Open-Meteo（无 key，前台 30 分钟节流，无位置回落种子假天气）；位置卡片 `react-native-maps` + Nominatim 搜索 + expo-location 反地理编码；位置只存本机。
* **账号与云**：`lib/auth.ts`（Supabase：Apple 主打 + 邮箱 OTP；匿名会话只作代理凭证、**不算登录**）+ `lib/sync.ts`（云端为主：整份快照 ↔ `snapshots`，15 s 防抖上传，启动 / 登录 / 回前台对账；**新设备或换账号时本机不许覆盖云端**：本机空静默拉，本机有关系则登录界面问「接回云端的 / 用本机覆盖云端」；决策纯函数 `planReconcile`，`tests/sync.test.ts`）。登录界面 `app/auth.tsx` 三入口：设置 / 首次入册强制墙 / onboarding「已有账号」。共享角色池 `lib/pool.ts`（`shared_characters`，同语言、5 分钟节流）。建表 `docs/supabase-setup.sql`。
* **关系数据**：心动值 `SquareChat.heart`（试聊 / 广场 / 自创暧昧期共用）+ `encounters` 广场偶遇记录（≤5），配对 3 天过期（自创不过期）；羁绊 `Bond`（`affinity` XP、`memory`、`notes` TA 的记事本、`circle` / `circleChats` 身边的人、`phoneCode` / `phoneUnlocked`、`birthday` 回落）；帖子 `Post.comments` 的 `from` 含 `other`（别人）、`reacted` 已生成互动；世界书 `worldBooks` / `worldFavorites`，角色 `worldId`；曲线与槽位在 `lib/bond.ts`；约定 `store.outingPlans`（`at` / `source`，窗口前 2h～后 3h，`lib/appointments.ts`）；外出 `store.outingSession`（一小时冷却）；相册 `store.album`；发帖 / 记事本各一只钟（`postSchedule` / `noteSchedule`，MBTI 定频）；桌面 `desktopSlots` / `desktopDock`；身份 `me` / `meByCharacter`；语言 `language`；壁纸即主题 `wallpaper`（`themeId` 退役，persist v6）。
* **i18n**：`lib/i18n.ts` 中文原文即键，`t()` + en / ja / ko 词典（尾部各自哨兵前追加），缺词回落中文；日期 / 数字格式化统一 `localeOf()`；切换全局 remount；内容层四语（中 / 英 / 日 / 韩）见 `content/characters/`，韩语与日 / 英一样待母语写手润色（#23）；百度语音不支持日 / 韩，走 OpenAI 兼容通道。
* **设计系统**：Claude Design 原文 `design/design-system.page.html`，全屏设计稿 `design/Everylove Paper UI.html` + 说明 `design/README.md`；色彩 = `THEMES.paper`（唯一配色；壁纸给它换 bg / accentSoft / line 三档 `applyPaperTint`），规格 = `constants/design.ts`；27 屏已全部按设计稿重做（D-100）：primitive = `AppScreen` / `HeaderAction`、`Card` / `Divider`、`Button`、`Chip` / `Segmented`、`Field` / `Input`、`DiamondBackground` / `ChatWallpaper`、`CharAvatar`（方块 r6 + 衬线首字）、`Polaroid`、`showToast`；palette 外只允许通话深底与 TA 记事本米色。App 图标 / 启动图 / favicon 由 `scripts/logo/render.mjs` 从 SVG 渲染（纸面粉底 + 微倾的小手机、不写字，D-103），改图只改脚本再重出。
* **测试与工具**：`npm test`（vitest：prompt 快照 + 回合管线 + 引擎工具 + 本地化 + 词典覆盖 + 台词 + 同步决策 + D-110 一组）、`npm run typecheck`、`npm run lint`（含 React Compiler 的 hooks 规则：渲染期不读 ref、effect 里不同步 setState、渲染期无 `Date.now()`——`reactCompiler` 已开，这些规则真修不关；Animated 值用 `useAnimatedValue` / `useAnimatedValueXY`）；设置 → 开发者：引擎点选切换、取路只读、记忆库查看（可强制提取）、立绘生成 / 重画、重置。
