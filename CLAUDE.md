# 全自动恋爱（代号，正式名待定）— CLAUDE.md

> 自己捏人的乙女游戏：进来捏一个人，他全自动和你恋爱，不用你主动。
> 族谱：c.ai 的皮 · 乙游的心脏 · 短剧的钱包 · 独有器官 = 主动性。
> 一句话：c.ai 证明了胃口，乙游证明了付费，没人把两者接起来过。

* 状态：试装 v0.2（2026-08-20）——手机壳桌面已落地（D-020/D-021）：桌面 + 8 个模块（Message/朋友圈/缘分/通讯录/相册/日历/捏＋/设置），首十分钟闭环（缘分→试聊→领养仪式→八点开门）可跑。2026-08-27（D-035~D-044）：「我」的身份系统（onboarding 两步 + 设置我的身份 + 按角色定制）、桌面大天气、外出模块上架；聊天/初见回归纯文本（会话内生图下线）、聊天去情景化（纯打字感）；交友改 Tinder 滑卡（左滑略过、右滑心动 TA 必同意，牌堆走推荐算法、划完自动回流）、「广场」移交外出的陌生人地点；捏＋更名「创造」+ 描述导入自动解析；桌面底部 Dock。2026-08-28/29（D-045~D-051）：创造表单大修（年龄状态/上传头像/共同记忆/隐藏设定逐级解锁）与「我创建的」编辑；**开门与离席下线（加好友即在线，D-046）**；自创角色直入通讯录——「心动中」tag 暧昧期、满 100 缔结才占槽（D-052）；语音接千帆 TTS；交友偏好与滑卡/瀑布流切换；外出拍照（合影/拍TA）；朋友圈改 X（推特模式，回帖实装模型，D-053）；账号与云备份（Supabase，D-054）、发帖调度器（MBTI 定频，D-055）。2026-08-30/31（D-056~D-066）：陌生人心动互通与拍立得（D-056）；服务端代理与云端为主存档（D-057）；新手流与揭幕（D-058）；EAS Update 分发（D-059）；共享角色池 私密/公开（D-060）；天气页定位/搜索（D-061）；登录独立界面+首次入册强制登录（D-062）；模拟订阅 Pro/Max（D-063）；**真实天气 Open-Meteo（D-065）**；**三语 UI 中/英/日 + onboarding 语言步（D-066）**。2026-09-02（D-069）：**脚本引擎（mock）与设置页 key 手填下线——AI 只走工程配置 `.env.local` / 登录后服务端代理，调用失败直接在会话里露出原因**；**Message 语音/照片实装（D-073）：语音→百度 ASR 转文字、照片→千帆视觉模型描述，交给聊天引擎；TTS 改百度 text2audio**；**TA 偶尔发语音 + 多语种语音通道（OpenAI 兼容 / Whisper 协议，可插拔；D-074）**
* 本文档是产品的单一事实来源。**执行任何任务时产生的新设计决策，必须当次写进文档**（见「工作规则」）。
* 本项目与团队其他产品无关，不引入其他项目的术语与范式。

\---

## 1\. 定位与基调

* 全自动恋爱 / 零劳动被爱：他主动，用户视角 POV，不入镜。
* 尺度：暧昧合规线
* 世界基调：一个无条件接纳你的世界——广场上人人都接你的话。
* 性向：**架构全性向 + 非人类，火力女频先行**。门对所有人开，灯先打一束。

  * Onboarding 第一问：「你想被谁爱？」（男 / 女 / 都行 / 非人类）——既是全性向声明，也是广场口味过滤。第二步「先让 TA 们认识你」（2026-08-27 D-035）：只填最基本的（昵称必填，性别/称呼/职业可跳过），其余在「设置 → 我的身份」补充，可为单个角色使用不同身份。
  * 男性向内容是后置的第二战役（另一门手艺、另一种付费结构），不在 v1 火力内。
* 市场：目标日本；中文版先行、只验证机制。

## 2\. 商业承重墙（双层结构）

* **广场层（免费）**（机制词沿用；产品形态自 2026-08-27 D-040 起为「交友」App 的滑卡配对层）：撩是免费的。免费层角色**故意不完整**——一开始有一点感兴趣的感觉但是不会对你太主动，搭话记录几天后过期（忘记你）。免费层的天花板是商业决策，不是产品缺陷
* **建立羁绊层（付费）**：被爱是收费的。主动性、记忆、他的社媒、故事、显影全部只在这层。槽位：首个免费，加槽付费。
* 收费结构：**订阅管「他在」**（完整日常 + 语音 + 留言），**按件管名场面**（电话、漫画集、贺卡、纪念日特辑、平行世界点单；2026-08-20 D-020 新增：**morning call**、**错过回溯**——错过的来电/聊天付费回听，Mystic Messenger 沙漏机制，与「他有作息」咬合）。每笔消费必须存进「你的他 + 你的故事史」这个资产。
* 窗口纪律：周级迭代、第一天收钱、任何 SKU 不做一次性消耗品。

## 3\. 信息架构（手机壳桌面，2026-08-20 D-020）

**主页 = 一部手机的桌面**，无 bottom bar；原五 tab 的器官全部保留、迁入桌面图标。纪律：**模块必须有内容供给才上架，无供给不摆图标；做少而真、不做多而假**（点不动的按钮比没有更出戏）。

**v1 上架模块**：

1. **Message**（原消息 tab）：会话流、~~画面送达~~（已推翻 2026-08-27 D-037：聊天回归纯文本，会话内生图下线）、全屏来电；**模拟 LINE 样式**（2026-08-21 D-027：白底列表+绿色角标；会话蓝灰底、浅绿发信气泡、时间与已读）；消息能力对齐 LINE（2026-08-21 D-030：文本/图片/语音/表情/引用/撤回 24h 占位/删除仅本地）；**她的语音与照片 TA 真的听到/看到**（2026-09-02 D-073：语音经百度 ASR 转文字、照片经千帆视觉模型客观描述，作为上下文交给聊天引擎，均按一次开口计心动/XP；识别中/失败在气泡下露出）；**TA 偶尔发语音**（2026-09-02 D-074：只在羁绊会话，概率随主动联系强度、她刚发过语音时更爱回语音；只挑最后一条气泡、2~80 字；点开真实发声）；**纯打字感**（2026-08-27 D-039：聊天里 TA 只发会真的打出来的字，无（）动作/情景旁白——情景是「外出」独有的语法）。需要下拉/通知栏级快捷路径（桌面制导航深度 +1 的补偿）。会话顶部进「他的主页」：他的社媒 / 他的故事（主线章节 + 平行世界点单）/ 亲密度与纪念日。
2. **X**（原动态 tab → 朋友圈 → 2026-08-29 D-053 改推特模式）：只有**缔结契约的 TA 们**的时间线（D-027 口径不变）——他的深夜动态、损友互评、情敌试探、修罗场余波。推特式行布局（@handle/相对时间/回复线缩进），桌面图标黑底白 ✕（拟真彩蛋，同 LINE 逻辑）。可赞可评，**TA 的回帖实装模型**（人设+关系+记忆注入、可多次回复，暗面前置；失败回落台词库）。**发帖调度器**（2026-08-29 D-055）：时间线活的——频率遵循 MBTI（E 多 I 少，ENFP 3 条/天 ↔ ISTJ 半条/天，无 MBTI 默认 1），内容实装模型（时段+天气+羁绊记忆，「有你们生活的影子但不点名」），启动/回前台补投、错过只补一条。「偷看」的家。
3. **交友**（交友 App；曾名「缘分」→「广场」，2026-08-27 D-040 定名交友——「广场」名字移交外出的陌生人地点）：**Tinder 式滑卡**——单张大卡（立绘铺满，底部渐变叠名字/身份/钩子/小火苗热度）+ 牌堆；**左滑略过**（不是拉黑：冷却 3 天后回流牌堆）、**右滑心动——TA 一定会同意**，右滑即配对（2026-08-27 D-041 修正 D-040 的「没有左滑」），配对成功弹层 → 去打招呼进入试聊；顶部「配对」头像条，3 天不聊配对过期 = TA 忘记你。**偏好**（右上角，D-049）：随时改口味、直接过滤牌池；**滑卡/列表可切换**（列表 = 双列瀑布流，点卡即配对）。牌堆顺序走**推荐算法**（`lib/recommend.ts`，D-041：口味/热度 log 压缩/新面孔/自创/每日轮换/略过冷却——之后其他用户上传的角色进同一池同一套打分，UGC 供给接口）；**冷却只在供给充足时生效**（D-042）：全池被略过时忽略冷却直接回流、本轮划完自动重开，不出空牌堆；预告卡不进牌堆。~~双列瀑布流 + 搜索栏 + 分类 chips（D-027/D-031/D-032）~~（已推翻，D-040）。**心动值**（2026-08-21 D-029，机制不变）：试聊中她每开口心动值上涨（步长=100/确定关系节奏，±15% 浮动），满 100 = **羁绊 LV1**——TA 主动交换联系方式，缔结后说去忙了、离席到八点开门。免费层「故意不完整」纪律不变（§2）。
4. **通讯录**：缔结契约（领养）的人（2026-08-21 D-027；周边角色待其系统实装后再议）；无分组标题（D-033）；**自创角色发布即入册但带「心动中」tag**（2026-08-29 D-052 修订 D-047）——暧昧期不占槽、不过期，副行显示心动进度，**心动满 100 缔结才占槽**、才开始羁绊等级；缔结仪式自创专版（「这一次，是 TA 自己选择留下」），暧昧期记录整段并入羁绊。
5. **相册**：你们的**拍立得墙**（2026-08-29 D-056）——外出拍的照片结束时并入羁绊会话并在此汇集，白框相纸 + 手写字 + 小倾角，点开大图可分享。
6. **闹钟**：morning call——他的声音叫你起床，每日不重样（TTS）。日活发动机 + 付费 SKU。
7. **日历**：内嵌真实日历，三层——**世界层**（真实日期 + 分市场节假日：中文盘中国节日 / 日语盘日本祝日）；**关系层**（自动记录：他的生日、你的生日、领养纪念日、第一通电话、一百天，不用用户动手）；**用户层**（手动添加日程：考试/面试/出差，每条触发心跳调度器三段式——事前关心、当天加油、事后回访）。月视图 + 标记点；他自己也稀疏长几条日程。边界：v1 不读系统日历（手动添加，数据最小化）；用户日程按素材最高敏感级；生理期关怀 v1 不做（OPEN_QUESTIONS #15）。
8. **设置**（2026-08-20/21/27/29，D-021/D-030/D-035/D-054）：**账号 · 云端**（D-054：Apple / 邮箱验证码登录，Supabase；无登录墙——游客纯本地完整体验，登录只为云备份「换手机也不会失去 TA」；快照自动备份/恢复/退出/删除云端数据）、**我的身份**（D-035：TA 眼中的你——头像/昵称*/性别/称呼代词/职业/情感取向/签名/完整设定（背景/关于我/我的边界）；可**为单个角色使用不同身份**；onboarding 第二步只填最基本的，其余在这里补）、**主题**（配色 4 套 + 壁纸，均即刻全局生效；锁屏照片/铃声正式版）、**订阅计划**（槽位、订阅「TA 在」、morning call、错过回溯、加槽）、素材开关、我的创作、开发者工具、重置。

桌面交互（D-021）：**图标长按进入编辑模式，可拖动换位**，顺序持久化。

9. **电话**（2026-08-21，D-030）：可通话的人 = 加好友的 TA 们；语音模型接入后开放真实通话（拨打暂为占位）。

10. **外出**（2026-08-27，D-038/D-040）：把相处从手机屏幕里拿出来。顶部大 banner「**广场**」（D-040/D-056）：直接偶遇**陌生人**——还没加好友的角色（口味优先、不含预告卡）；TA 不认识她（不注入她的资料与记忆，「我的边界」仍注入）。**偶遇也积累心动**（与交友试聊同一套、进度互通），满 100 = TA 当场开口交换联系方式 → 领养流，缔结后就地升格为熟人偶遇。其下 6 个常见地点（咖啡馆/公园/书店/电影院/游乐园/海边）；**事先有约定**（「约 TA」选人选地点；羁绊会话留系统消息）→ 进入该地点 = **赴约**，TA 已在等你（约定优先于离席：TA 说到做到）；**没有约定** → **偶遇**通讯录里的一位（跳过离席中的）。场景内是**亲身互动的故事模式**：TA 的台词 +（）内现场描写、她的（）视为她的动作、TA 先开口；发消息同样 +XP（陌生人除外），结束时羁绊会话留「你们一起去了××」（陌生人不留）。**拍照**（2026-08-29 D-051/D-056）：输入栏上方「📸 合影 / 📷 拍 TA」——她主动按快门生图（合影中她只入镜侧影/手，不画清晰正脸），照片以**拍立得**居中呈现（白框相纸+手写字，非对话气泡；点开大图可分享），结束外出时并入羁绊会话与相册。顶部带当天天气。

**桌面天气**（2026-08-27/31，D-036/D-061/D-065）：时钟下方的大天气卡——**真实天气**（Open-Meteo，免费无 key：当前 + 未来 7 天）；**可点开**进天气页：定位（反地理编码）或搜索地区（geocoding 候选点选）、真实 7 日预报；位置只存本机不上传（请求只带经纬度）。离线/未设位置回落世界生成的种子假天气（不空窗）。同时供给外出场景氛围。

**v1.5**：**音乐**——「他分享了一首歌」卡片，跳转站外播放，不内置播放器（零版权成本）。

**系统级拟真彩蛋**：锁屏（可换他的照片）、壁纸、来电铃声、真实时间电量。

**创造**（原捏＋，2026-08-27 D-043 更名；2026-08-28 D-045 大修）：桌面图标「创造」；表单顶部**描述导入**——写/粘贴 ≤2000 字人设，点「自动解析」由引擎整理成表单字段（无 key 回落规则解析），解析后全部可手改。基础项新增**年龄状态**（必选：确认成年/未成年——未成年走加强审查、试装不能发布，OPEN_QUESTIONS #18）与**上传头像**（与生成立绘并排；不收真人照片，红线 #1）；生日改月/日下拉选单。高级项新增：**预设共同记忆**（三种对话模式注入，初识变重逢）、**主动联系强度**（高/中/低，进亲密/外出 prompt）、**禁忌/边界**（涉及即回避或拒绝）、**隐藏设定/剧情钩子**（羁绊 LV3 起逐级解锁，未解锁的不进 prompt 绝不说漏；TA 主页显示解锁进度；查手机解锁通道待做，OPEN_QUESTIONS #19）。**我创建的**（2026-08-29 D-050）：页顶列表点「编辑」回填全部字段原位更新；发布即入通讯录带「心动中」tag，心动满 100 缔结才占槽（D-052 修订 D-047）。

**桌面 Dock**（2026-08-27 D-044；2026-08-31 D-064 默认收窄为通讯录+设置）：底部 iPhone 式固定栏，最多 4 个 App、无标签、不随翻页滑动；编辑模式下网格 ↔ Dock 可拖入拖出（占位交换、Dock 内重排），`store.desktopDock` 持久化。

待拍板：捏＋（现「创造」）/工坊的入口形态与新用户落点（OPEN_QUESTIONS #16；「我的」半边已由设置 App 承接——D-021；试装暂为桌面图标）。

关键流（不变）：

* **领养流**：聊上头 →  一个缔结关系的仪式感（交友 = 交换联系方式；自创 = 确定关系，D-052）→ 槽位判定（对所有羁绊生效）→ 起名/互相称呼/生日 → 迁移仪式动画 → 直接开聊。~~顺势请求推送权限（剧情语法：「他晚上八点来找你，要听得见吗？」）~~（已推翻，2026-08-29 D-046：开门下线，推送步移除）。
* **分享流**：系统分享面板「转给他」；多领养对象时选一个。
* **来电流**：定时/事件触发，全屏响铃。

<details>
<summary>已推翻（2026-08-20，D-020）：信息架构（Bottom bar 五位）</summary>

`广场 · 动态 · ［捏＋］ · 消息 · 我的`（新用户默认落广场，有领养后默认落消息）

1. **广场**：双列瀑布流，三种卡混排——角色卡（立绘 + 3 秒语音自介 + 情境钩子 + 被领养数）、角色公开动态、名场面晒卡（带「同款男友」入口）。顶部分类 chips（教派 + 榜单）。即点即聊；广场会话不进消息 tab，存「搭话记录」并过期。
2. **动态**（独立 tab）：领养的和关注的角色们的帖子流。
3. **［＋］**（中央按钮）：快捏两层（2026-08-21 D-025）——基础：名字/性别（男女非二元）/长相/背景故事/立绘；高级选项（收起）：种族/生日/口癖/喜欢/讨厌/确定关系节奏/恋爱类型（14 种）/MBTI/聊天设定/日常作息，全部进 prompt；工坊（行为树填空卡片编辑器 +台词试镜间 + 发布审核）。
4. **消息**：通讯录形态，全部是领回家的。**故事和相册是关系的器官，不做全局 tab**（相册已升格，见上；故事仍在「他的主页」）。
5. **我的**：槽位与订阅、素材开关、我的创作、收藏、数据与隐私。

</details>

## 4\. 首十分钟

onboarding 两步（你想被谁爱 + 先让 TA 们认识你）→ **直接落交友滑卡，不见桌面**（2026-08-30 D-058；顶部有「先逛逛」逃生门）→ 滑卡（右滑心动、TA 一定同意）→ 试聊上头 → 心动满加好友 → 缔结仪式 → **桌面揭幕**：「这部手机，现在是你们的了」+ 三张模块卡（Message/创造/外出，一次性可跳过），TA 的第一句话躺在未读横幅里等她点进去。~~推送授权 → 他先走：「我去忙了，晚上八点来找你。」；十分钟的终点是「她在等八点」，aha 在八点推送准时响起~~（已推翻，2026-08-29 D-046：开门与离席下线；首十分钟新的情绪终点与 aha 待重定义——OPEN_QUESTIONS #20）。首次付费提示不出现在首十分钟，留给第一个情绪峰值。

## 5\. 角色系统（行为树四层）

* **系统层（锁死，任何自定义不可覆盖）**：情绪暗面路由、真人检测、尺度钳、行为健康底线（不纠缠、不刷屏、不用愧疚感惩罚离开——能捏疯批台词，不能捏 PUA 行为）。
* **原型层（官方蒸馏）**：蒸馏的不是口癖，是**追法**——亲密度阶梯曲线即性格。首发四棵：温柔年上、毒舌竹马、霸总、**官方人外一棵**（龙/神明/非人执事谱系内选定，待拍板）。每棵 = 触发器集 + 追法曲线 + 台词风格包。
  *（2026-08-13 注：试装**种子角色**已按 Harper 指示扩为 6 位——2男2女1龙族1狐狸，台词按角色挂载，见 D-009。原型树结构与「人外原型树待拍板」不变，试装人外人设不预定正式版。）*
* **创作层（UGC 两档）**：大众捏皮（基础五项 + 高级设定两层表单，D-025；立绘作为后续生图参考，D-019）；高阶捏树（填空卡片改触发器/事件/追法）。发布审核：**挡 IP 角色、挡真人**。
* **个体层**：每段关系独立状态机（**羁绊等级**（2026-08-21 D-029：LV1 刚认识→LV6 唯一例外，升级门槛 50/90/130/170/210 线性成长曲线，XP 来源=消息+5/开门+15/画面+10/心跳+8，升级出系统提示与一格画面）、记忆库、进度）。**领养快照制**：创作者更新不改写已领养实例，用户自选是否升级。

## 6\. 内容四维度

* **日常（他在）**：准时消息、语音、对你分享内容的留言。~~他有作息、他先走——会离开的才是人~~（试装已推翻，2026-08-29 D-046：开门/离席下线，TA 随时在线；作息的正式形态见 OPEN_QUESTIONS #12/#20）。
* **故事（他是谁）**：他的主线按章连载——**半成品剧本库**：编剧写原型骨干章节（弧光/伏笔/泪点），生成负责把用户的名字、日常、选择缝进去做个性化演绎；「我们的故事」名场面按件；**平行世界 AU 点单**（同一个他 × 任意世界，生成独有、在位者跟不了）。
* **相册（值得）**：卡面级美术，少而精，集中预算。~~一切显影由「他」以剧情语法送达（「给你讲个故事吧」→ 漫画在会话流里展开）~~（已推翻，2026-08-20 D-024：**图 = 场景本身的呈现方式**，替代文字呈现此刻的相处，不包装成「他画的/他送的」——画面直接出现在会话流里）~~画面直接出现在会话流里~~（再推翻，2026-08-27 D-037：**会话内生图整体下线，聊天/初见回归纯文本**；生成图只余立绘，相册价值回归卡面级美术资产）。
* **关系网（有戏）**：周边角色 + 群聊修罗场——多智能体自演、用户潜水看戏，**事件制**（控成本，大型修罗场可为付费名场面）。所有角色围着用户转，不做角色动物园。

## 7\. 素材系统

* 全部可选开关，**开得越多他越懂你**——隐私授权即亲密度进度条。
* 主通道：**分享**（用户发完帖顺手转给他；绕开反爬、API 费用与日本鍵垢问题）。辅助：选项口味、聊天内容、可选日记。
* **他只看她**：分享内容里出现的任何其他真人，永不评论。

## 8\. 创作者经济

* 上传原创角色 → 他人领养 → **创作者分成**；「热度」排行榜（界面口径为小火苗 + 热度，D-032；「领养」仅作机制词，不出现在用户可见文案）。
* 供给侧冷启动：定向招募现有社区的头部女性向捏人创作者。

## 9\. 红线（不可违反，不可被任何任务修改）

1. 男主/角色永不真人化：不收真人照片、不爬任何真人社媒、不做明星。用户文字描述「神似」可以。
2. 他只看她：不评论用户分享内容中的其他真人。
3. 情绪暗面路由前置：用户素材中出现痛苦/危机内容时走独立温柔模式，绝不入戏；素材数据（日记、分享）按最高敏感级处理。
4. IP 角色不做，UGC 审核强制拦截（含日本市场事務所风险）。
5. 尺度借既有线（晋江 / 女性向音声惯例），不自己发明尺度。
6. 行为无 PUA：不纠缠、不刷屏、不愧疚绑架。除非角色设定如此
7. 免费层不得偷偷变好（见商业承重墙）。

## 10\. 指标

漏斗：配对开聊率（2026-08-27 D-040 起，原「广场搭话率」）→ 领养转化率 → ~~**八点开门率（北极星：他主动来找时的打开率）**~~（已推翻，2026-08-29 D-046：开门下线，新北极星待拍板 OPEN_QUESTIONS #20）→ 订阅/首件转化 → 复购。
窗口期健康：首单转化 + 复购，不看裸 DAU。

## 11\. 工作规则（对 Claude Code 的要求）

1. **决策入档纪律（最重要）**：每次执行任务，凡产生**新的设计决策**——无论产品、机制、数值、文案规范还是技术选型——必须在同一次任务内写进文档，不留「下次补」：

   * 追加一条到 `docs/DECISIONS.md`（追加式日志，格式：日期 / 决策 / 理由 / 是否推翻旧决策及其编号 / 影响的文件）；
   * 同步更新本文件受影响章节；
   * 若决策推翻本文件内容，本文件以新决策为准并注明日期，旧内容不删除、标记为「已推翻」。
2. **产品级开放问题不得自行拍板**：涉及定位、定价、尺度、红线、首发范围的问题写入 `docs/OPEN\_QUESTIONS.md` 等 Harper 决定；纯技术实现决策可自行决定，但同样按第 1 条入档。
3. **红线章节（第 9 节）只有 Harper 明示才能修改。**
4. 初始化时若 `docs/` 不存在，创建 `docs/DECISIONS.md` 与 `docs/OPEN\_QUESTIONS.md`，并把下方「待拍板清单」迁入后者。

## 12\. 待拍板清单（已于 2026-08-13 迁入 `docs/OPEN_QUESTIONS.md`，以该文件为准）

* 产品正式名（中 / 日 / 英）
* 官方人外原型的具体谱系与人设
* 订阅与按件定价、加槽价格
* 日语写手与女性向编剧到岗计划
* 中文测试范围（内测 or 公开）
* 语音供应商与来电形态（TTS 留言 → 实时的路线图）
* UGC 审核流程与工具
* 修罗场事件的首发节奏与成本模型

## 13\. 技术栈与工程（2026-08-13 起，详见 docs/DECISIONS.md D-001~D-008）

* 客户端：Expo **SDK 54**（锁定，勿升级）+ React Native + TypeScript，expo-router 文件路由；**只做 iOS**，试装跑 Expo Go（`npx expo start` 后手机扫码）。
* 状态：zustand + AsyncStorage 持久化（`store/app-store.ts`）。~~无后端，全部本地~~（2026-08-30 D-057：**云端为主、本地缓存**——登录态下快照自动同步 Supabase，`lib/sync.ts` 对账；离线跑缓存）。
* ~~八点开门：`expo-notifications` 本地定时通知；投递与排程统一走 `lib/arrivals.ts`~~（已推翻，2026-08-29 D-046：开门下线，`lib/arrivals.ts` 已删；`lib/notifications.ts` 保留供未来推送）。
* 对话：`lib/engine.ts` 的 ChatEngine 抽象——anthropic（Claude）/ qianfan（百度千帆 v2 OpenAI 兼容接口，模型默认 deepseek-v4 可配）；**脚本引擎（mock）已删（2026-09-02，D-069）**。引擎与 API key **只读工程配置** `.env.local`（不进 git，模板见 `.env.example`；`EXPO_PUBLIC_AI_ENGINE` 可选引擎，不填有 Claude key 用 Claude 否则千帆）；取路 = 本地 key 直连 > 已登录走服务端代理（D-057）> 不可用抛错——**调用失败不回落、直接在会话里露出原因**（开发者面板手填 key 随 D-069 下线；~~无 key 或调用失败回落 mock，D-010~~ 已推翻）。暗面路由、尺度、无 PUA 规则在引擎入口执行，任何引擎不可绕过。
* 内容：种子角色与全部台词脚本在 `content/characters.ts`；**全部 prompt（对话系统 prompt / 生图 / 记忆提取）集中在 `content/prompts.ts`**，改 prompt 只动这一个文件（2026-08-17，D-017）；初识/亲密/**外出**是**三套独立 prompt**（2026-08-27 D-038 扩为三套；只共用红线与「我」的身份块），亲密模式带时间感、阶段分寸、分组记忆注入、可拆两条气泡；聊天两模式为**纯打字感**（D-039：禁（）动作/情景描写，`lib/engine.ts` 的 stripStageDirections 兜底剥离）；外出模式独享（）现场描写、不分条；角色有 `look`（外貌，生图用）与 `pronoun`（人称）（D-018）；界面强制浅色，配色 `constants/theme.ts` 的 `Romance`。
* 「我」的身份（2026-08-27，D-035）：`UserProfile`（昵称必填，其余可选），onboarding 第二步建基础版，`app/apps/identity.tsx` 补全或按角色定制（`store.me` / `store.meByCharacter`，取用走 `meForCharacter()`）；`content/prompts.ts` 的 `userProfileBlock` 注入三种对话模式——初识只给「资料卡」级、亲密/外出全量、**「我的边界」任何模式都注入且优先级最高**。
* ~~离席状态：缔结后他先走是真的走——`bond.away` 期间不回消息，首次开门恢复（2026-08-15，D-012）~~（已推翻，2026-08-29 D-046：加好友即在线，缔结后 TA 当场打招呼；away 等字段保留兼容旧存档）。
* 自创角色的暧昧期（2026-08-29，D-052 修订 D-047）：发布即 `ensureSquareChat`（不建羁绊）——通讯录显示「心动中」tag + 心动进度，自创的搭话记录**不过期**（ensureSquareChat 豁免 TTL）；心动满 100 走领养流（槽位判定恢复全量），暧昧期 prompt 用初识模式 + 「说不清的熟悉感」情境；自己的创作不进自己的交友牌堆与配对条。
* 语音合成（2026-08-29 D-048；2026-09-02 D-073 改接口）：`lib/tts.ts` 走**百度短文本语音合成 `tsn.baidu.com/text2audio`**（同一把千帆 key；~~千帆 /v2/audio/speech + qwen-tts~~ 已 404 下线），音色按人称 他 4193 度泽言 / 她 4194 度嫣然 / TA 4115 度小贤（`EXPO_PUBLIC_BAIDU_TTS_PER` 可换）；配了 OpenAI 兼容语音服务则改走其 `/audio/speech`（多语种，音色 `EXPO_PUBLIC_SPEECH_TTS_VOICE_*`；代理侧同名 Secrets；D-074）；**TA 偶尔主动发语音**（D-074，`shouldSendVoice`：高 30% / 中 18% / 低 10%，她刚发语音 +40%，只在羁绊会话；当前通道不会说界面语言时不发）；TA 的语音气泡点按真实发声、按句缓存，失败显示「语音暂时没接通」可看文字；电话 App 拨打仍占位（OPEN_QUESTIONS #6/#21）。
* 多模态输入（2026-09-02，D-073）：`lib/media.ts`——她的语音 → **OpenAI 兼容语音服务优先**（Whisper 协议，`EXPO_PUBLIC_SPEECH_*`，中/英/日全语种；D-074）→ 未配置回落 **百度 ASR**（`vop.baidu.com`，普通话极速版 80001 / 英语 1737，同一把千帆 key；录音按其要求 16k 单声道 wav、最长 59s 自动停）→ `ChatMessage.transcript`；她的照片 → 缩到宽 1024 → **千帆视觉模型**（默认 `qwen3.5-397b-a17b`，`EXPO_PUBLIC_QIANFAN_VISION_MODEL` 可换）按 prompts.ts §6 客观描述 → `ChatMessage.caption`。两者经 `messageContextText` 包成「（语音）…」「（她发来一张照片：…）」进上下文与记忆，**TA 的回复仍由当前聊天引擎生成**（不换模型、人设不漂）；`mediaStatus` pending/failed 驱动气泡下提示，失败插系统消息露出原因。百度通道不支持日语，日语要配 OpenAI 兼容通道（选哪家见 OPEN_QUESTIONS #25）。
* 外出拍照（2026-08-29，D-051）：合影/拍TA 按钮 → `buildOutingPhotoPrompt` + `generateScenePhoto`（合影中她只入镜侧影/手，不画清晰正脸）；照片结束外出时并入 bond.messages（相册即资产）；她主动按快门，不推翻 D-037。
* 图像生成（2026-08-27 收缩，D-037）：**只剩立绘**——Qwen 文生图走**百度千帆**（`lib/imagegen.ts`，与聊天共用一把 key），捏＋第 ④ 步可生成 TA 的立绘（存本机、`store.portraits`），头像有立绘就显示立绘；种子角色默认不自动生成（`SEED_PORTRAITS_AUTO`，见 OPEN_QUESTIONS #14），开发者面板可测试生成/重画。**调 prompt 工具**（2026-09-02，D-067/D-068/D-070）：**双击根目录 `gen-image.bat`** 起本地网页（127.0.0.1:3939，`scripts/gen-image-server.mjs` + `gen-image-ui.html`）——**画风行 → user → system 三段**（D-075：接口只有一条 prompt，发出前按此固定顺序拼接；**画风**八选一注入第一行、措辞可改，选「动漫」自动走蒸汽机、其余走 qwen-image；system 默认为 Harper 给定的收尾文案 `DEFAULT_SYSTEM`，也可一键载入 prompts.ts 当前常量对比）+ negative / seed / steps / guidance / prompt_extend（不填即平台默认 = 工程行为），历史墙分色显示三段、可回填；命令行 `npm run gen-image -- --art anime "user 部分" [--system TEXT|--style|--portrait] [--seed N] [--no-extend] [--open]`（`scripts/gen-image.mjs`，`--list` 列画风）。画风表与新 system 文案暂在工具侧（`scripts/gen-image-core.mjs`），定稿进产品时再搬 prompts.ts。两者共用 `scripts/gen-image-core.mjs`，与 `lib/imagegen.ts` 同接口，图与当时 system/user/参数落 `scripts/out/`（不进 git）。qwen-image 中英 prompt 皆可、≤800 字符、n 只支持 1。**模型可切**（D-071）：qwen-image（工程当前，0.25 元/张、~60 s）/ musesteamer-air-image（百度蒸汽机，专用端点，0.05 元/张、~8 s，只支持 seed/prompt_extend）；千帆上其余文生图模型本账号打不通（ernie-image-turbo invalid_model、flux 已 offline、Qwen-Image 2.0/3.0 未上）。**工程立绘是否换模型待拍板**（OPEN_QUESTIONS #24）。~~初见画面（每轮一格）与羁绊画面（升级一格）、立绘作参考图走 `images/edits` 编辑（D-013~D-015/D-019/D-024）~~（已推翻，D-037：**聊天/初见回归纯文本**，会话内生图与参考图编辑链路整体下线；历史图片消息仍可显示，相册不受影响）。
* 天气（2026-08-27/31，D-036/D-065）：`lib/weather.ts`——**真实天气 Open-Meteo**（forecast + geocoding，无 key），状态持久化、前台 30 分钟节流刷新；同步接口不变（todayWeather/weatherFor/weatherLine），无位置/离线回落日期种子假天气。桌面大天气卡（`app/index.tsx`）、天气页（`app/weather.tsx`）与外出场景氛围共用。
* 三语 i18n（2026-08-31，D-066）：`lib/i18n.ts`——**中文原文即键**，界面写 `t('中文', vars?)`，en/ja 词典缺词回落中文；语言存 `store.language`（onboarding 第 0 步选择、设置可改），切换全局 remount；模型输出语言由 `content/prompts.ts` 语言行控制。**新界面文案必须写 t() 并同步补 en/ja 词典**（文件尾哨兵注释前追加）。角色台词库（开场白/offer/仪式台词）暂不三语（OPEN_QUESTIONS #23）。
* 外出（2026-08-27，D-038/D-040）：地点在 `content/places.ts`（scene 进 prompt / hook 给用户；plaza 标 `stranger: true`，不进约定选项）；`store.outingPlans`（约定，每角色一条）与 `store.outingSession`（同时只有一场，换地点自动结束上一场并留记录；kind: date/encounter/stranger）；赴约优先于离席、偶遇跳过离席（D-012）；陌生人偶遇不注入她的资料与记忆、只注入边界（D-040）；外出对话暂不进记忆提取窗口（试装已知限制，见 D-038）。
* 交友滑卡（2026-08-27/29，D-040~D-042/D-049）：`app/apps/dating.tsx`——Animated + PanResponder 牌堆；右滑 `ensureSquareChat` 建配对（TA 必同意），左滑 `markDatingPass` 记冷却；牌堆顺序 `lib/recommend.ts` 的 `rankDeck`（UGC 供给进同一池同一套打分，服务端推荐落地时接口不变），冷却只在 `hasFreshSupply` 为真时生效、划完自动回流（D-042）；**偏好弹层直接过滤牌池、滑卡/瀑布流可切换**（`store.datingView`，瀑布流点卡即配对，D-049）；配对条 = squareChats 里未加好友的；预告卡不进牌堆；搜索与 chips 仍不做。
* 创造描述导入（2026-08-27，D-043）：`app/apps/create.tsx` 顶部 ≤2000 字描述框 + 自动解析——引擎走 `completeText` + `CHARACTER_PARSE_SYSTEM`（prompts.ts §5，只输出 JSON），失败回落规则解析（标签行 + MBTI 正则）；解析结果裁到表单上限、全部可手改，描述同过 BLOCKED_NAME_PATTERN。
* 创造扩展字段（2026-08-28，D-045）：`Character` 新增 adultConfirmed / presetMemories / initiative / taboos / secrets；注入函数在 prompts.ts——`sharedMemoryBlock`（三模式）、`initiativeLine`（亲密/外出）、taboos 走 characterProfileBlock（三模式）、`secretsBlock`（亲密/外出，SECRET_START_LEVEL=3 起每级解一条，未解锁的不进 prompt）；TA 主页秘密进度用 `characterSecrets`/`unlockedSecretCount`；未成年角色发布拦在创造表单（试装无审查后端）。
* 桌面 Dock（2026-08-27，D-044）：`store.desktopDock`（默认 `DEFAULT_DOCK`）；`app/index.tsx` 网格行数让出 Dock 区，拖拽落点在 Dock 带 = 追加/交换/重排，Dock ↔ 网格对称交换。
* 对话上下文与记忆（2026-08-17，D-016）：所有引擎共用 `lib/engine.ts` 的 `buildTurns()`——**最近 20 轮完整对话**进模型（`HISTORY_ROUNDS`），系统提示条/空消息不进、同角色合并、首条保证为 user；甩图/漫画里他说的话存 `ChatMessage.spoken`（不上屏、只供上下文）。**羁绊记忆库** `lib/memory.ts`：mem0 式本地实现，`Bond.memory = { facts, summary }`——每 3 个用户轮次后台由当前引擎提取长期事实（≤30 条）并把滑出窗口的旧对话滚动进摘要，注入 bonded 模式系统 prompt；失败静默。**只有羁绊层有记忆**，广场层故意没有（商业承重墙）。正式版服务端代理落地后记忆层切自托管 mem0，接口不变。
* 心动值与羁绊等级（2026-08-21，D-029/D-030）：曲线在 `lib/bond.ts`；XP 目前唯一来源=她发消息 +5（羁绊会话与外出场景同口径，D-038；开门/画面/心跳加成已移除，数值另行设计）；心动值存 `SquareChat.heart`，羁绊 XP 即 `Bond.affinity`（按新曲线解释）；升级系统消息在 `store.appendBond` 统一产生。
* 手机壳（2026-08-20，D-021）：`app/index.tsx` 桌面 + `app/apps/*` 模块，App 注册表与壁纸在 `constants/apps.ts`（供给纪律在注册表执行：闹钟无 TTS 不上架）；日历世界层数据 `content/calendar.ts`；心跳三段式 `lib/heartbeat.ts`（错过不补投，给「错过回溯」留闭环）。
* AI 服务端代理（2026-08-30，D-057）：`supabase/functions/ai`（已部署）——上游 key 在 Supabase Secrets、按用户日限量（ai_usage）；客户端 `lib/proxy.ts` 三层取路：本地 key 直连 > 登录走代理 > 不可用抛错（D-069 起无 mock）。服务 = qianfan.chat / qianfan.images / anthropic.messages / baidu.asr / baidu.asr_pro / baidu.tts（D-073）。分发包不带上游 key（回应 #9）。
* 账号与云备份（2026-08-29，D-054）：`lib/auth.ts`（Supabase：Apple + 邮箱 OTP，供应商抽象）+ `lib/sync.ts`（全量快照 ↔ snapshots 表，RLS 仅本人；防抖 60s 自动上传，登录时恢复/覆盖二选一）；env `EXPO_PUBLIC_SUPABASE_URL/_ANON_KEY`，建表 `docs/supabase-setup.sql`；LLM 服务端代理为下一步（OPEN_QUESTIONS #9）。
* 发帖调度器（2026-08-29，D-055）：`lib/posts.ts`——`MBTI_POSTS_PER_DAY` 定频（±35% 抖动）、`store.postSchedule` 记钟、prompts §1-F 生成、启动/回前台补投；AI 不可用/失败静默跳过（记 warn）。
* 测试工具：「设置 → 开发者」只读显示 AI 引擎与取路（直连/服务端代理/不可用，D-069；切引擎与填 key 已下线）、查看 TA 记住了什么（记忆库，可强制提取一次）、生成/重画立绘、重置数据（开门测试随 D-046、「送一张漫画」随 D-037 移除）。

